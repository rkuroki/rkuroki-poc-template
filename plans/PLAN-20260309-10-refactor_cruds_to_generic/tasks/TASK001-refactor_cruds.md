# TASK001: Refactor Existing CRUDs

1. **Refactor Users**: Edit `app/home/users/page.tsx` and `app/actions/user.ts` (if needed) to map to `CrudLayout`.
2. **Refactor PageUrls**: Edit `app/home/pageurls/page.tsx` and `app/actions/pageurls.ts` (if needed) to map to `CrudLayout`.
3. **Refactor Notes**: Edit `app/home/notes/page.tsx` and `app/actions/notes.ts` (if needed) to map to `CrudLayout`.
